FROM node:20-alpine
# Railway healthcheck requires curl inside the runtime image.
RUN apk add --no-cache curl ca-certificates && command -v curl && curl --version
WORKDIR /app
COPY package*.json ./
RUN npm install --omit=dev
COPY . .
ENV NODE_ENV=production
ENV PORT=8080
CMD ["node","./server.js"]
