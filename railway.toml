[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.homestro-fixed"

[deploy]
startCommand = "sh -c 'node ./patch-autopilot.js && node ./patch-shopify-auth.js && node ./patch-json-safe.js && node ./server.js'"
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ALWAYS"

# Homestro fixed deployment trigger 2026-09-16
