[build]
builder = "RAILPACK"

[deploy]
startCommand = "node ./patch-autopilot.js && node ./server.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ALWAYS"
restartPolicyMaxRetries = 10
sleepApplication = false
