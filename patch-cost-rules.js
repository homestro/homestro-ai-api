// Legacy cost-rules patch intentionally disabled.
// Qualification is handled directly by patch-autopilot.js using
// MAX_PRODUCT_COST, MIN_SELLING_PRICE and MIN_PRICE_COST_RATIO.
console.log('cost rules patch disabled');
